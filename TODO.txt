Menu
	Button
		void onMouseDown(sf::Vector2u)
		void onMouseUp(sf::Vector2u)
		void onMouseIn(sf::Vector2u)
		void onMouseOut(sf::Vector2u)

	TextInput  /* Add TextInput *m_focusInput; to App */
		void onMouseDown(sf::Vector2u)
		void onMouseUp(sf::Vector2u)
		void onMouseIn(sf::Vector2u)
		void onMouseOut(sf::Vector2u)
		void onKeyDown(sf::Keyboard::Key)
		bool m_hasFocus



##
## Chat-room de ouf
##

## Objectifs

# Editor
	Rework keybinds (display help w/ F1)
	Rewrite level file structure
		[binary format]
		Header magic 'NETlvl$$' [$$:format version]
		Level name, type?, size,

	Sub rooms (houses)

# Client
	Mise en cache des map et tileset

# Misc
	Update utils.make
	CMakeLists ?